# ToDo-List

Personal to-do app: priority + due dates + tags, quick capture + subtasks.

# How to Run

```
pip install -r requirements.txt
python3 webapp.py
```

Then open http://localhost:5000. Data persists in `todo.db`
